version: "3.9"

services:
  api:
    build: .
    container_name: api_proteccion
    restart: always
    env_file:
      - .env
    ports:
      - "3001:3001"
    volumes:
      # Persistencia de uploads
      - /api/uploads:/app/uploads
